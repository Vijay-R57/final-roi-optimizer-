# Sample Drop Optimization

Run `python run.py`, then `uvicorn api.main:app --reload` and `streamlit run dashboard/app.py`.
The supplied records are synthetic; ROI is explicitly projected, not realized.
